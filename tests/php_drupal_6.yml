httpd:
  image: viljaste/httpd:dev
  hostname: httpd
  ports:
    - "80"
    - "443"
  volumes_from:
    - httpdata
  links:
    - php
httpdata:
  image: viljaste/data:dev
  hostname: httpdata
  volumes:
    - /httpd
mysqld:
  image: viljaste/mysqld:dev
  hostname: mysqld
  volumes_from:
    - mysqlddata
mysqlddata:
  image: viljaste/data:dev
  hostname: mysqlddata
  volumes:
    - /mysqld
php:
  image: viljaste/php:5.2-dev
  hostname: php
  volumes_from:
    - httpdata
  links:
    - mysqld
  environment:
    - DRUPAL_VERSION=6
